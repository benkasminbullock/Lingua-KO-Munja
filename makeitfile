all:    Makefile
	make -f Makefile

Makefile:    Makefile.PL
	perl Makefile.PL

clean:
	-make -f Makefile clean
	-rm -f Makefile Makefile.old
	-rm -rf Lingua-KO-Munja-0.*
	purge

# Local variables:
# mode: makefile
# End:
